class Solution {
public:
    int robRange(vector<int> &nums, int start, int end) {
        int firsh = nums[start], second = max(nums[start], nums[start + 1]);
        for(int i = start + 2; i <= end; i++) {
            int temp = second
        }

    }

    int rob(vector<int> &nums) {

    }
}
